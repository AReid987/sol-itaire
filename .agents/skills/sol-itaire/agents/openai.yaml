interface:
  display_name: "Sol Itaire"
  short_description: "Repo-specific patterns and workflows for sol-itaire"
  default_prompt: "Use the sol-itaire repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true